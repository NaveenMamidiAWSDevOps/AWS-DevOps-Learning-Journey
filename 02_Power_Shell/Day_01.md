Introduction:

Shell is a program , it will takes the commands whatever user i styping and will gives to the OS to execute.

Shell is an Interface Between User and OS;
 so the common shell is Bash { Bourne Again Shell }, which is default shell in all the Advanced Linux OS.

 ex : cat /etc/shell { so this command shows all the command line shells installed and allowed on your system }
 where 
     cat : Displays the Content of the File ;
     /etc/shell : A System file that lists all the valid login shells.


so now,
   SHELL SCRIPTING means a file containing a series of commands . the shell reads this file and carriers out the commands as through they have been entered directly on the Commander line.

   Normally the SHEll scripts has the file extension  ".sh"
# WHy Shell Scripting ?
the main Benefit or the advantage of using shell scripting was " Automating the Regualarities "
 so they are :
  1. Automate your Regulat JOBS;
  2. Taking DATABASE Backups
  3. Monitoring the several Server Resources like CPU utilization.
  4. Portable { it can be executed in any unix -like OS Without any Modifications }
     